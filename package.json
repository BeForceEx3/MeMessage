{
  "name": "meme-chat",
  "version": "1.0.0",
  "description": "Анонимный чат MeMessage",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "dev:env": "nodemon -r dotenv/config server/index.js",
    "render-build": "echo 'No build required'",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.5.0",
    "uuid": "^9.0.0",
    "helmet": "^7.0.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^6.10.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
